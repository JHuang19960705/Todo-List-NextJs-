<!DOCTYPE html>
<html lang="zh-Hant">

<head>
  <meta charset="UTF-8" />
  <title>編輯代辦事項</title>
  <link rel="stylesheet" href="/css/styles.css" />
</head>

<body>
  <h1>編輯代辦事項</h1>

  <form method="POST" action="/update_todoList/<%= todo.id %>?_method=PATCH" class="todo-input-grid">
    <input name="name" value="<%= todo.name %>" required />
    <input type="date" name="due_date" value="<%= new Date(todo.due_date).toISOString().split('T')[0] %>" required />
    <button type="submit">儲存變更</button>
  </form>

  <a href="/">回清單</a>
</body>

</html>
